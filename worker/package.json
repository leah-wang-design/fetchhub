{
  "name": "design-feedback-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create design-feedback-db",
    "db:migrate": "wrangler d1 migrations apply design-feedback-db",
    "db:migrate:local": "wrangler d1 migrations apply design-feedback-db --local"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "wrangler": "^3.95.0"
  }
}
